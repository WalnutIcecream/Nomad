"""The bundled Nomad desktop application (the "hub").

Ships the controller, an embedded PostgreSQL, a relay server and the
launcher UI behind a single entry point so end users run one app.
"""

from __future__ import annotations